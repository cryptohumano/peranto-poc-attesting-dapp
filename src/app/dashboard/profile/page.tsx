'use client';

import TabsNav from '@/app/components/TabsNav';

const Profile = () => {
  return (
    <TabsNav defaultIndex={0}>
      <p>Profile Here</p>
    </TabsNav>
  );
};

export default Profile;
